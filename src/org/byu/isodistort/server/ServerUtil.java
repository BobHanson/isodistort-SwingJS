package org.byu.isodistort.server;

import java.util.function.Consumer;

import org.byu.isodistort.local.IsoApp;

public class ServerUtil {
	 private ServerUtil() {
		 // no instance; static only
	 }
	 
	 // <form action="isodistortuploadfile.php" method="POST" enctype="multipart/form-data"
	 
	String publicServerURL = "" ;
	public void updateIsovizF(IsoApp app, Object formData, Consumer<String> callback) {
		
		 
		 
	 }
}